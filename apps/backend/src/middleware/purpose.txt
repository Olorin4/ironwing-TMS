This directory contains reusable Express middleware functions.

Middleware in this folder is intended to be shared across different features to handle common tasks like authentication checks, logging, or request validation.