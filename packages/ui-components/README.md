# UI Components

This package contains a collection of reusable UI components that can be shared across the desktop and mobile applications.

## Components

*   **Button:** A standard button component.
*   **Card:** A simple card component for displaying content.

## Usage

To use a component from this package, import it into your application like this:

```javascript
import { Button, Card } from 'ui-components';